"use strict";

const http = require("http");
const fs = require("fs");
const path = require("path");
const { execFile } = require("child_process");
const { promisify } = require("util");
const { CallbackTargetRegistry, validateCallbackTargetId } = require("./callback-target-registry");
const { BrowserPageBindingStore, pageIdentityFromUrl, pagesMatchingBinding } = require("./browser-page-binding");
const { BrowserRuntimeRegistry } = require("./browser-runtime-registry");
const { setBrowserCallbackTarget } = require("./set-browser-callback-target");
const crypto = require("crypto");

const execFileAsync = promisify(execFile);
const DEFAULT_PORT = 8787;
const CLI_SESSION = "relay-poc";
const CHATGPT_PAGE_ORIGINS = Object.freeze(["https://chatgpt.com", "https://www.chatgpt.com", "https://chat.openai.com"]);
const BOUND_NAVIGATION_ORIGINS = CHATGPT_PAGE_ORIGINS;
const CALLBACK_STATUSES = Object.freeze(["DONE", "FAILED", "BLOCKED"]);
const EVENT_SCHEMA = Object.freeze({ schema_version: 1, event: "task_terminal", action: "REVIEW" });
const CALLBACK_REVIEW_INSTRUCTION = "请使用 Slack 读取此 task_id 对应的终态 CODEX_STATUS 消息，获取完整执行信息（包括 token_usage）；本事件仅用于提醒。";
const CHANNELS = Object.freeze(["msedge", "chrome"]);
const BROWSER_CONTEXT_OWNERSHIPS = Object.freeze(["attached_user_browser", "relay_managed"]);
const DEFAULT_MANAGED_PROFILE_PATH = path.join(process.cwd(), ".agent-relay", "browser-profile");
const DEFAULT_MANAGED_START_URL = "https://chatgpt.com/";

function parseHttpUrl(rawUrl) { try { const url = new URL(String(rawUrl || "")); return ["http:", "https:"].includes(url.protocol) && !url.username && !url.password ? url : null; } catch { return null; } }
function isAllowedChatGPTOrigin(rawUrl) { const url = parseHttpUrl(rawUrl); return Boolean(url && CHATGPT_PAGE_ORIGINS.includes(url.origin)); }
function isAllowedBoundNavigation(rawUrl) { const url = parseHttpUrl(rawUrl); return Boolean(url && BOUND_NAVIGATION_ORIGINS.includes(url.origin)); }
function targetIdentity(rawUrl) { const url = parseHttpUrl(rawUrl); return url ? `${url.origin}${url.pathname}` : null; }
function validateCallback({ task_id: taskId, status } = {}) { if (!/^[A-Za-z0-9][A-Za-z0-9._-]{0,63}$/.test(String(taskId || ""))) throw new Error("callback_task_id_invalid"); if (!CALLBACK_STATUSES.includes(status)) throw new Error("callback_status_invalid"); return { task_id: String(taskId), status }; }
function optionalSlackReference(callback = {}) { const reference = {}; if (/^F[A-Z0-9]+$/.test(String(callback.evidence_file_id || ""))) reference.evidence_file_id = String(callback.evidence_file_id); if (/^[CGD][A-Z0-9]+$/.test(String(callback.slack_channel_id || ""))) reference.slack_channel_id = String(callback.slack_channel_id); if (/^\d+\.\d+$/.test(String(callback.slack_status_ts || ""))) reference.slack_status_ts = String(callback.slack_status_ts); try { const permalink = new URL(String(callback.evidence_permalink || "")); if (permalink.protocol === "https:" && (permalink.hostname === "slack.com" || permalink.hostname.endsWith(".slack.com")) && !permalink.username && !permalink.password) reference.evidence_permalink = permalink.toString(); } catch {} return reference; }
function renderAgentRelayEvent(callback) { const { task_id: taskId, status } = validateCallback(callback); return `${JSON.stringify({ ...EVENT_SCHEMA, task_id: taskId, status, ...optionalSlackReference(callback) })}\n\n${CALLBACK_REVIEW_INSTRUCTION}`; }
function validateAttachEndpoint(rawEndpoint) { let endpoint; try { endpoint = new URL(String(rawEndpoint || "")); } catch { endpoint = null; } if (!endpoint || !["ws:", "http:"].includes(endpoint.protocol) || !["127.0.0.1", "localhost", "::1"].includes(endpoint.hostname) || endpoint.username || endpoint.password || endpoint.search || endpoint.hash) throw new Error("attach_endpoint_must_be_explicit_local_cdp_endpoint"); if (endpoint.protocol === "ws:" && !/^\/devtools\/browser\/[^/]+$/.test(endpoint.pathname)) throw new Error("attach_ws_endpoint_must_be_browser_cdp_endpoint"); if (endpoint.protocol === "http:" && endpoint.pathname !== "/") throw new Error("attach_http_endpoint_must_be_loopback_root"); return endpoint.toString(); }
function validateChannel(channel) { if (!CHANNELS.includes(channel)) throw new Error("attach_channel_must_be_msedge_or_chrome"); return channel; }
function validateBrowserContextOwnership(value) { if (!BROWSER_CONTEXT_OWNERSHIPS.includes(value)) throw new Error("browser_context_ownership_invalid"); return value; }
function validateBrowserContextId(value) { const contextId = String(value || ""); if (!/^context-[A-Za-z0-9-]{1,55}$/.test(contextId)) throw new Error("browser_context_id_invalid"); return contextId; }
function createBrowserContextDescriptor({ context, ownership, contextId = `context-${crypto.randomUUID()}` } = {}) { if (!context || typeof context.pages !== "function") throw new Error("browser_context_required"); return Object.freeze({ id: validateBrowserContextId(contextId), ownership: validateBrowserContextOwnership(ownership) }); }
function validateManagedProfilePath(value) { const configured = String(value || ""); if (!configured || !path.isAbsolute(configured)) throw new Error("browser_profile_path_invalid"); const profilePath = path.resolve(configured); if (profilePath === path.parse(profilePath).root) throw new Error("browser_profile_path_invalid"); return profilePath; }
function managedContextId(profilePath) { return `context-${crypto.createHash("sha256").update(process.platform === "win32" ? profilePath.toLowerCase() : profilePath).digest("hex").slice(0, 32)}`; }
function validateManagedStartUrl(rawUrl) { const url = parseHttpUrl(rawUrl); if (!url || !isAllowedChatGPTOrigin(url.toString()) || url.search || url.hash) throw new Error("browser_start_url_must_be_allowlisted_chatgpt_url"); return url.toString(); }
function createBrowserTargetDescriptor({ page, startUrl } = {}) { if (!page || typeof page.url !== "function") throw new Error("browser_target_page_required"); return Object.freeze({ type: "start_url", url: validateManagedStartUrl(startUrl), page }); }
function validateRelayCallbackUrl(rawUrl) { const url = parseHttpUrl(rawUrl); if (!url || url.protocol !== "http:" || !["127.0.0.1", "localhost", "::1"].includes(url.hostname) || url.username || url.password || url.search || url.hash || url.pathname !== "/api/callback") throw new Error("callback_url_must_be_explicit_loopback_api_callback"); return url.toString(); }
function parseAttachArguments(args, environment = process.env) { const channelIndex = args.indexOf("--channel"); const endpointIndex = args.indexOf("--endpoint"); if (channelIndex >= 0 && endpointIndex >= 0) throw new Error("attach_channel_and_endpoint_are_mutually_exclusive"); if (channelIndex >= 0) { if (channelIndex !== 0 || !args[channelIndex + 1] || args.length !== 2) throw new Error("attach_arguments_invalid"); return { mode: "channel", channel: validateChannel(args[channelIndex + 1]) }; } const configured = endpointIndex >= 0 ? args[endpointIndex + 1] : environment.AGENT_RELAY_BROWSER_CDP_ENDPOINT; if (!configured || (endpointIndex >= 0 && (endpointIndex !== 0 || args.length !== 2))) throw new Error("attach_endpoint_required_use--channel_msedge_or_chrome_or_AGENT_RELAY_BROWSER_CDP_ENDPOINT"); return { mode: "endpoint", endpoint: validateAttachEndpoint(configured) }; }
function parseManagedArguments(args, environment = process.env) { let configuredProfile; let configuredStartUrl; for (let index = 0; index < args.length; index += 2) { const flag = args[index]; const value = args[index + 1]; if (!value || !["--profile", "--start-url"].includes(flag)) throw new Error("managed_arguments_invalid"); if (flag === "--profile") { if (configuredProfile) throw new Error("managed_arguments_invalid"); configuredProfile = value; } else { if (configuredStartUrl) throw new Error("managed_arguments_invalid"); configuredStartUrl = value; } } const profilePath = validateManagedProfilePath(configuredProfile || environment.AGENT_RELAY_BROWSER_PROFILE_PATH || DEFAULT_MANAGED_PROFILE_PATH); const startUrl = configuredStartUrl || environment.AGENT_RELAY_BROWSER_START_URL || DEFAULT_MANAGED_START_URL; return { profilePath, startUrl: validateManagedStartUrl(startUrl) }; }
function validateAttachedBrowser(browser) { if (!browser || typeof browser.contexts !== "function") throw new Error("attach_browser_invalid"); const contexts = browser.contexts(); if (contexts.length !== 1 || !contexts[0] || typeof contexts[0].pages !== "function") throw new Error("attach_browser_context_invalid"); return contexts[0]; }
async function attachToExistingBrowser({ chromium, endpoint }) { if (!chromium || typeof chromium.connectOverCDP !== "function") throw new Error("playwright_connect_over_cdp_unavailable"); const browser = await chromium.connectOverCDP(endpoint, { isLocal: true, noDefaults: true }); return { browser, context: validateAttachedBrowser(browser) }; }
function managedAuthenticationState(page) { return page && isAllowedChatGPTOrigin(page.url()) ? "ready" : "authentication required"; }
function logManagedBrowserLifecycle(log, message) { log(`[BROWSER] ${message}`); }
async function createRelayManagedBrowser({ chromium, profilePath, startUrl, lifecycleLog = console.log, pageBindingStore } = {}) { if (!chromium) throw new Error("playwright_chromium_launch_unavailable"); if (!profilePath) { if (typeof chromium.launch !== "function") throw new Error("playwright_chromium_launch_unavailable"); let browser; try { browser = await chromium.launch({ headless: true }); } catch (error) { if (!/Executable doesn't exist/.test(error.message || "")) throw error; browser = await chromium.launch({ channel: "msedge", headless: true }); } const context = await browser.newContext(); return { browser, context, browserContext: createBrowserContextDescriptor({ context, ownership: "relay_managed" }), close: () => browser.close() }; } if (typeof chromium.launchPersistentContext !== "function") throw new Error("playwright_persistent_context_unavailable"); const persistentProfilePath = validateManagedProfilePath(profilePath); const persistentStartUrl = validateManagedStartUrl(startUrl || DEFAULT_MANAGED_START_URL); const bindingStore = pageBindingStore || new BrowserPageBindingStore(); fs.mkdirSync(persistentProfilePath, { recursive: true }); logManagedBrowserLifecycle(lifecycleLog, "launching visible Chromium"); logManagedBrowserLifecycle(lifecycleLog, `profile path: ${persistentProfilePath}`); let context; try { context = await chromium.launchPersistentContext(persistentProfilePath, { headless: false }); } catch (error) { if (/Executable doesn't exist/.test(error.message || "")) context = await chromium.launchPersistentContext(persistentProfilePath, { channel: "msedge", headless: false }); else if (/Target page, context or browser has been closed/.test(error.message || "")) throw new Error("browser_profile_in_use_close_existing_managed_browser"); else throw error; } const savedBinding = bindingStore.get(persistentProfilePath); let page; let pageBinding; if (savedBinding) { const matches = await pagesMatchingBinding(context, savedBinding.identity); if (matches.length === 1) { ({ page } = matches[0]); pageBinding = { status: "RESTORED", identity: savedBinding.identity, page }; logManagedBrowserLifecycle(lifecycleLog, "page binding restored"); } else { pageBinding = { status: matches.length ? "AMBIGUOUS" : "REBIND_REQUIRED", identity: savedBinding.identity, page: null }; logManagedBrowserLifecycle(lifecycleLog, matches.length ? "page binding ambiguous; rebind required" : "page binding missing; rebind required"); } } else { page = context.pages()[0]; if (!page) page = await context.newPage(); await page.goto(persistentStartUrl); pageBinding = { status: "UNBOUND", identity: null, page: null }; } const authenticationState = managedAuthenticationState(page || context.pages()[0]); logManagedBrowserLifecycle(lifecycleLog, authenticationState); return { context, browserContext: createBrowserContextDescriptor({ context, ownership: "relay_managed", contextId: managedContextId(persistentProfilePath) }), browserTarget: page ? createBrowserTargetDescriptor({ page, startUrl: persistentStartUrl }) : null, pageBinding, pageBindingStore: bindingStore, profilePath: persistentProfilePath, startUrl: persistentStartUrl, authenticationState, close: () => context.close() }; }
function npxCliEntrypoint() { return path.join(path.dirname(process.execPath), "node_modules", "npm", "bin", "npx-cli.js"); }
function cliLaunchSpec(commandArgs, platform = process.platform) { const args = ["--yes", "--package=@playwright/cli@latest", "playwright-cli", `-s=${CLI_SESSION}`, ...commandArgs]; return platform === "win32" ? { executable: process.execPath, args: [npxCliEntrypoint(), ...args] } : { executable: "npx", args }; }
async function runPlaywrightCli(commandArgs, runner = execFileAsync) { const launch = cliLaunchSpec(commandArgs); const result = await runner(launch.executable, launch.args, { cwd: process.cwd(), shell: false, windowsHide: true, maxBuffer: 1024 * 1024 }); return result.stdout || ""; }
function extractTabs(output) { const text = String(output); return [...text.matchAll(/#\s*Tab\s+(\d+)\s*-\s*(https?:\/\/[^\s<>\"]+)/g)].map((match) => ({ index: Number(match[1]), title: "", url: match[2] })).concat([...text.matchAll(/^\s*-\s*(\d+):\s*(?:\(current\)\s*)?\[([^\]]*)\]\((https?:\/\/[^\s)]+)\)/gm)].map((match) => ({ index: Number(match[1]), title: match[2], url: match[3] }))).sort((left, right) => left.index - right.index); }
function extractUrls(output) { return extractTabs(output).map(({ url }) => url); }
function extractSnapshotTextboxRef(snapshot) { const match = String(snapshot).match(/^\s*- textbox(?: "[^"]*")? \[ref=(e\d+)\]/m); return match ? match[1] : null; }
function extractChatInputRef(snapshot) { const match = String(snapshot).match(/^\s*- textbox "(?=[^"]*(?:Message|ChatGPT))[^"]*" \[ref=(e\d+)\]/m); if (!match) throw new Error("chatgpt_composer_not_found"); return match[1]; }
async function resolveChatComposer(runner) { const selectors = ["textarea[placeholder*='Message']", "textarea[data-id='root']", "div[contenteditable='true'][data-lexical-editor='true']", "div[contenteditable='true']"]; for (const selector of selectors) { const snapshot = await runPlaywrightCli(["snapshot", selector], runner).catch(() => ""); const ref = extractSnapshotTextboxRef(snapshot); if (ref) return { ref, selector }; } const ref = extractChatInputRef(await runPlaywrightCli(["snapshot"], runner)); return { ref, selector: "role=textbox[name*=ChatGPT|Message]" }; }

function createCliContext({ channel, runner } = {}) {
  validateChannel(channel); let entries = [];
  const adapter = {
    async refresh() { const tabs = extractTabs(await runPlaywrightCli(["tab-list"], runner)); const previous = new Map(entries.map((entry) => [entry.identity, entry])); const next = []; for (const tab of tabs) { const identity = targetIdentity(tab.url); const duplicate = next.some((entry) => entry.identity && entry.identity === identity); const entry = (!duplicate && identity && previous.get(identity)) || { identity, title: tab.title, url: tab.url, index: tab.index, page: null }; entry.identity = identity; entry.title = tab.title; entry.url = tab.url; entry.index = tab.index; next.push(entry); } entries = next; return entries; },
    async revalidate(entry) { await adapter.refresh(); const matches = entries.filter((candidate) => candidate.identity === entry.identity); const current = matches.find((candidate) => candidate.index === entry.index) || (matches.length === 1 ? matches[0] : null); if (!current || !isAllowedChatGPTOrigin(current.url)) throw new Error("send_target_revalidation_failed"); entry.url = current.url; entry.index = current.index; return entry; },
    pages() { return entries.map((entry) => entry.page || (entry.page = { url: () => entry.url, title: async () => entry.title, targetIdentity: () => entry.identity, locator: () => ({ first: { fill: async (value) => { await adapter.revalidate(entry); await runPlaywrightCli(["tab-select", String(entry.index)], runner); try { const composer = await resolveChatComposer(runner); const result = await runPlaywrightCli(["fill", composer.ref, value], runner); console.log("[FOUND COMPOSER]", { selector: composer.selector, visible: true, enabled: true, origin: new URL(entry.url).origin }); return result; } catch (error) { await runPlaywrightCli(["screenshot"], runner).catch(() => {}); throw error; } }, press: async (key) => { await adapter.revalidate(entry); await runPlaywrightCli(["tab-select", String(entry.index)], runner); return runPlaywrightCli(["press", key], runner); } } }) })); },
  };
  return { adapter, context: { pages: () => adapter.pages() } };
}
async function attachViaPlaywrightCli({ channel, runner } = {}) { const selected = validateChannel(channel); await runPlaywrightCli(["attach", `--cdp=${selected}`], runner); const { adapter, context } = createCliContext({ channel: selected, runner }); await adapter.refresh(); return { context, adapter, close: async () => {} }; }
function html() { return `<!doctype html><html lang="zh-CN"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Agent Relay Browser Callback PoC</title><style>body{font:16px system-ui,sans-serif;max-width:900px;margin:2rem auto;padding:0 1rem;color:#17202a}section{border:1px solid #ccd5df;border-radius:8px;padding:1rem;margin:1rem 0}button{padding:.55rem .8rem;margin:.25rem;cursor:pointer}input,select{padding:.5rem;margin:.25rem}pre{background:#f4f6f8;padding:.75rem;overflow:auto;white-space:pre-wrap}.muted{color:#5f6b76}</style></head><body><h1>Browser Callback PoC</h1><p class="muted">仅展示 allowlisted ChatGPT 页面；Bind 与 Arm 必须分别执行。mock callback 只渲染冻结的 AGENT_RELAY_EVENT v1，不代表用户授权。</p><section><h2>1. Browser context 页面</h2><p id="browser">正在连接…</p><p>Browser context：<code id="context">正在识别…</code></p><p>Page Binding：<code id="pageBinding">正在识别…</code></p><div id="pages"></div><button id="refresh">刷新页面列表</button></section><section><h2>2. Mock callback</h2><form id="callback"><label>task_id <input name="task_id" required pattern="[A-Za-z0-9][A-Za-z0-9._-]{0,63}"></label><label>status <select name="status"><option>DONE</option><option>FAILED</option><option>BLOCKED</option></select></label><button>渲染事件</button></form><pre id="event">尚未收到 mock callback。</pre><p>状态：<span id="arm">未绑定</span></p><p>Callback target：<code id="target">未注册</code></p><button id="armButton" disabled>Arm 已绑定对话</button><button id="send" disabled>发送到已绑定对话</button><p id="message" class="muted"></p></section><script>const $=id=>document.getElementById(id);async function state(){const r=await fetch('/api/state');const s=await r.json();$('browser').textContent=s.browser;$('context').textContent=s.browser_context.id+' · '+s.browser_context.ownership;$('pageBinding').textContent=s.page_binding?(s.page_binding.state+(s.page_binding.identity?' · '+s.page_binding.identity.origin+s.page_binding.identity.pathname:'')):'不适用';$('pages').replaceChildren();for(const p of s.pages){const row=document.createElement('p');row.textContent='Page '+p.id+' · '+p.url+(p.title?' · '+p.title:'')+' ';const b=document.createElement('button');b.textContent=s.bound_page_id===p.id?'已绑定':'Bind';b.disabled=s.bound_page_id===p.id;b.onclick=()=>bind(p.id);row.append(b);$('pages').append(row);}$('arm').textContent=s.armed?'已 Armed':(s.bound?'已 Bind，未 Armed':'未绑定');$('target').textContent=s.callback_target_id?(s.callback_target_id+' · '+s.callback_target_state):'未注册';$('armButton').disabled=!s.bound||s.armed;$('send').disabled=!(s.armed&&s.event);}async function bind(pageId){const r=await fetch('/api/bind',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({page_id:pageId})});const x=await r.json();$('message').textContent=x.message||x.error;await state();}async function arm(){const r=await fetch('/api/arm',{method:'POST'});const x=await r.json();$('message').textContent=x.message||x.error;await state();}$('refresh').onclick=state;$('armButton').onclick=arm;$('callback').onsubmit=async e=>{e.preventDefault();const body=Object.fromEntries(new FormData(e.target));const r=await fetch('/api/mock-callback',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)});const x=await r.json();$('message').textContent=x.message||x.error;$('event').textContent=x.event||'未生成事件';await state();};$('send').onclick=async()=>{const r=await fetch('/api/send',{method:'POST'});const x=await r.json();$('message').textContent=x.message||x.error;await state();};state();setInterval(state,3000);</script></body></html>`; }
function bindArmHtml() { return `<!doctype html><html lang="zh-CN"><meta charset="utf-8"><title>Agent Relay 一键授权</title><style>body{font:16px system-ui,sans-serif;max-width:720px;margin:3rem auto;padding:0 1rem;color:#17202a}button{padding:.7rem 1rem;font-size:1rem;cursor:pointer}li{margin:.7rem 0}.muted{color:#5f6b76}</style><h1>绑定并授权 ChatGPT 对话</h1><p class="muted">只会使用当前 Relay 管理的 Chrome 中明确列出的 ChatGPT 对话页面。</p><ol id="pages">正在读取页面…</ol><p id="message" class="muted"></p><script>const pages=document.getElementById('pages'),message=document.getElementById('message');async function load(){const r=await fetch('/api/state'),s=await r.json();pages.replaceChildren();if(!s.pages.length){pages.textContent='没有可绑定的 ChatGPT 对话页。';return}for(const p of s.pages){const li=document.createElement('li'),button=document.createElement('button');button.textContent='一键 Bind + Arm + 写入 .env';button.onclick=async()=>{button.disabled=true;message.textContent='正在授权…';const r=await fetch('/api/bind-arm-configure',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({page_id:p.id})});const x=await r.json();message.textContent=x.message||x.error||'操作失败';button.disabled=false};li.append(document.createTextNode((p.title||'ChatGPT 对话')+' '),button);pages.append(li)}}load()</script></html>`; }

function createCallbackServer({ context, browserContext, browserTarget = null, pageBinding = null, pageBindingStore = null, profilePath = null, refresh = async () => {}, host = "127.0.0.1", port = DEFAULT_PORT, debugLog = console.log, registry = new CallbackTargetRegistry(), configureCallbackTarget = setBrowserCallbackTarget } = {}) {
  if (profilePath) browserContext = createBrowserContextDescriptor({ context, ownership: "relay_managed", contextId: managedContextId(profilePath) });
  if (!context) throw new Error("browser_context_required"); const contextDescriptor = browserContext || createBrowserContextDescriptor({ context, ownership: "attached_user_browser" }); if (browserTarget && (!browserTarget.page || typeof browserTarget.page.url !== "function" || browserTarget.type !== "start_url")) throw new Error("browser_target_invalid"); if (pageBinding && (!pageBinding.identity && pageBinding.status !== "UNBOUND")) throw new Error("browser_page_binding_invalid"); if (pageBindingStore && (!profilePath || typeof pageBindingStore.save !== "function")) throw new Error("browser_page_binding_store_invalid"); if (!registry || typeof registry.register !== "function" || typeof registry.resolve !== "function" || typeof registry.setState !== "function") throw new Error("callback_target_registry_invalid"); let nextPageId = 1; const pageIds = new WeakMap(); let boundTarget = null; let armed = false; let event = null;
  async function pages() { return Promise.all(context.pages().map(async (page) => { if (!pageIds.has(page)) pageIds.set(page, nextPageId++); let title = ""; if (typeof page.title === "function") { try { title = await page.title(); } catch { title = ""; } } const url = page.url(); return { page, id: pageIds.get(page), url, title, identity: typeof page.targetIdentity === "function" ? page.targetIdentity() : targetIdentity(url) }; })); }
  async function allowedPages() { const allPages = await pages(); const debugPages = allPages.map(({ url, title }) => ({ url, title })); const allowed = allPages.filter(({ url }) => isAllowedChatGPTOrigin(url)); debugLog("all attached pages:", debugPages); debugLog("allowed pages:", allowed.map(({ url, title }) => ({ url, title }))); return allowed; }
  function setBoundTargetState(state) { if (boundTarget && boundTarget.callback_target_id) registry.setState(boundTarget.callback_target_id, state); }
  let restoreAttempted = false;
  async function restoreBoundTarget() {
    if (restoreAttempted || boundTarget || typeof registry.list !== "function") return;
    restoreAttempted = true;
    const currentPages = await allowedPages();
    const candidates = registry.list()
      .filter((target) => target.platform === "chatgpt" && target.state === "ARMED")
      .sort((left, right) => String(left.created_at).localeCompare(String(right.created_at)));
    const saved = candidates.at(-1);
    const entry = saved && currentPages.find((page) => page.identity === saved.conversation_identity);
    if (!entry) return;
    let target = saved;
    if (target.browser_context_id !== contextDescriptor.id) {
      registry.setState(target.callback_target_id, "EXPIRED");
      target = registry.register({ platform: "chatgpt", conversation_identity: entry.identity, browser_context_id: contextDescriptor.id });
      registry.setState(target.callback_target_id, "ARMED");
      configureCallbackTarget({ targetId: target.callback_target_id });
    }
    boundTarget = { page: entry.page, identity: entry.identity, callback_target_id: target.callback_target_id };
    armed = true;
  }
  async function boundState() { await restoreBoundTarget(); const current = (await pages()).find((entry) => boundTarget && entry.page === boundTarget.page && entry.identity === boundTarget.identity && isAllowedChatGPTOrigin(entry.url)); if (!current && boundTarget) { if (armed) armed = false; event = null; setBoundTargetState("EXPIRED"); } return current || null; }
  async function readJson(request) { let body = ""; for await (const chunk of request) { body += chunk; if (Buffer.byteLength(body) > 16 * 1024) throw new Error("request_too_large"); } return JSON.parse(body || "{}"); }
  function sendJson(response, statusCode, value) { response.writeHead(statusCode, { "content-type": "application/json; charset=utf-8", "cache-control": "no-store" }); response.end(JSON.stringify(value)); }
  async function resolveDeliveryTarget(callbackTargetId) { if (!callbackTargetId) return boundState(); const target = registry.resolve(validateCallbackTargetId(callbackTargetId)); if (!target || target.state !== "ARMED") throw new Error("callback_target_not_armed"); if (target.browser_context_id !== contextDescriptor.id) throw new Error("callback_target_context_mismatch"); const bound = await boundState(); if (!bound || !boundTarget || boundTarget.callback_target_id !== target.callback_target_id || bound.identity !== target.conversation_identity) throw new Error("callback_target_unavailable"); return bound; }
  async function sendRenderedEvent(callbackTargetId) { const bound = await resolveDeliveryTarget(callbackTargetId); if (!armed || !bound || !event) throw new Error("send_guard_blocked"); const verified = await resolveDeliveryTarget(callbackTargetId); if (!verified || verified.identity !== boundTarget.identity) throw new Error("send_target_revalidation_failed"); const selectors = ["textarea[placeholder*='ChatGPT']", "textarea[name='prompt-textarea']", "div[contenteditable='true'][data-lexical-editor='true']", "div[contenteditable='true']"]; let composer; for (const selector of selectors) { const locator = verified.page.locator(selector); const candidate = typeof locator.first === "function" ? locator.first() : locator.first; if (!candidate) continue; if (typeof candidate.isVisible !== "function" || await candidate.isVisible()) { composer = candidate; break; } } if (!composer) throw new Error("chatgpt_visible_composer_not_found"); await composer.fill(event); await composer.press("Enter"); }
  async function targetState() { if (!browserTarget) return null; const entry = (await pages()).find(({ page }) => page === browserTarget.page); return { type: browserTarget.type, url: browserTarget.url, browser_context_id: contextDescriptor.id, page_id: entry ? entry.id : null, state: entry && isAllowedChatGPTOrigin(entry.url) ? "OPEN" : "NOT_FOUND" }; }
  async function pageBindingState() { if (!pageBinding) return null; if (!pageBinding.identity) return { state: "UNBOUND", identity: null, page_id: null }; const matches = await pagesMatchingBinding(context, pageBinding.identity); const current = matches[0] && (await pages()).find(({ page }) => page === matches[0].page); return { state: matches.length === 1 ? "RESTORED" : (matches.length ? "AMBIGUOUS" : "REBIND_REQUIRED"), identity: pageBinding.identity, page_id: matches.length === 1 ? current.id : null }; }
  async function handle(request, response) { try { await refresh(); if (request.method === "GET" && request.url === "/") { response.writeHead(200, { "content-type": "text/html; charset=utf-8", "cache-control": "no-store" }); response.end(html()); return; } if (request.method === "GET" && request.url === "/api/state") { const currentPages = await allowedPages(); const bound = await boundState(); sendJson(response, 200, { browser: contextDescriptor.ownership === "relay_managed" ? "Relay 管理的独立浏览器 context；只展示该 context 内允许的 ChatGPT 页面。" : "已 attach 用户当前浏览器；只展示允许的 ChatGPT 页面。", browser_context: contextDescriptor, browser_target: await targetState(), page_binding: await pageBindingState(), pages: currentPages.map(({ id, url, title }) => ({ id, url, title })), bound_page_id: bound ? bound.id : null, callback_target_id: boundTarget ? boundTarget.callback_target_id : null, callback_target_state: boundTarget ? registry.resolve(boundTarget.callback_target_id)?.state || null : null, bound: Boolean(boundTarget), armed: Boolean(armed && bound), event }); return; } if (request.method === "POST" && request.url === "/api/bind") { const body = await readJson(request); const entry = (await allowedPages()).find(({ id }) => id === Number(body.page_id)); if (!entry) throw new Error("bind_requires_explicit_chatgpt_page"); if (pageBindingStore) { const saved = pageBindingStore.save({ profilePath, identity: pageIdentityFromUrl(entry.url, entry.title) }); pageBinding = { status: "RESTORED", identity: saved.identity, page: entry.page }; } if (boundTarget) setBoundTargetState("DISARMED"); const target = registry.register({ platform: "chatgpt", conversation_identity: entry.identity, browser_context_id: contextDescriptor.id }); boundTarget = { page: entry.page, identity: entry.identity, callback_target_id: target.callback_target_id }; armed = false; event = null; if (typeof entry.page.on === "function") entry.page.on("framenavigated", () => { if (boundTarget && entry.page === boundTarget.page && (!isAllowedChatGPTOrigin(entry.page.url()) || targetIdentity(entry.page.url()) !== boundTarget.identity)) { armed = false; event = null; setBoundTargetState("DISARMED"); } }); if (typeof entry.page.route === "function") await entry.page.route("**/*", async (route) => { if (route.request().isNavigationRequest() && route.request().frame() === entry.page.mainFrame() && !isAllowedBoundNavigation(route.request().url())) await route.abort("blockedbyclient"); else await route.continue(); }); sendJson(response, 200, { message: "已 Bind 当前 ChatGPT 对话；已保存该 managed profile 的页面 identity，请继续单独点击 Arm。", callback_target_id: target.callback_target_id, browser_context_id: contextDescriptor.id }); return; } if (request.method === "POST" && request.url === "/api/arm") { if (!(await boundState())) throw new Error("arm_requires_bound_chatgpt_target"); armed = true; setBoundTargetState("ARMED"); sendJson(response, 200, { message: "已 Arm 绑定的 ChatGPT 对话；现在允许发送。", callback_target_id: boundTarget.callback_target_id }); return; } if (request.method === "POST" && request.url === "/api/mock-callback") { if (!armed || !(await boundState())) throw new Error("arm_required_before_callback"); event = renderAgentRelayEvent(await readJson(request)); sendJson(response, 200, { message: "已渲染冻结的 AGENT_RELAY_EVENT v1；尚未发送。", event }); return; } if (request.method === "POST" && request.url === "/api/callback") { const body = await readJson(request); if (!armed || !(await resolveDeliveryTarget(body.callback_target_id))) throw new Error("arm_required_before_callback"); event = renderAgentRelayEvent(body); await sendRenderedEvent(body.callback_target_id); sendJson(response, 200, { message: "已渲染并发送冻结的 AGENT_RELAY_EVENT v1。", event }); return; } if (request.method === "POST" && request.url === "/api/send") { await sendRenderedEvent(); sendJson(response, 200, { message: "已通过绑定 ChatGPT 对话的可见输入框和 Enter 发送事件。" }); return; } sendJson(response, 404, { error: "not_found" }); } catch (error) { sendJson(response, 400, { error: error.message || "request_failed" }); } }
  async function handleBindArmConfigure(request, response) {
    if (request.method === "GET" && ["/", "/bind"].includes(request.url)) {
      response.writeHead(200, { "content-type": "text/html; charset=utf-8", "cache-control": "no-store" });
      response.end(bindArmHtml());
      return true;
    }
    if (request.method !== "POST" || request.url !== "/api/bind-arm-configure") return false;
    try {
      const body = await readJson(request);
      const entry = (await allowedPages()).find(({ id }) => id === Number(body.page_id));
      if (!entry) throw new Error("bind_requires_explicit_chatgpt_page");
      if (pageBindingStore) {
        const saved = pageBindingStore.save({ profilePath, identity: pageIdentityFromUrl(entry.url, entry.title) });
        pageBinding = { status: "RESTORED", identity: saved.identity, page: entry.page };
      }
      if (boundTarget) setBoundTargetState("DISARMED");
      const target = registry.register({ platform: "chatgpt", conversation_identity: entry.identity, browser_context_id: contextDescriptor.id });
      boundTarget = { page: entry.page, identity: entry.identity, callback_target_id: target.callback_target_id };
      armed = true;
      event = null;
      setBoundTargetState("ARMED");
      if (typeof entry.page.on === "function") entry.page.on("framenavigated", () => {
        if (boundTarget && entry.page === boundTarget.page && (!isAllowedChatGPTOrigin(entry.page.url()) || targetIdentity(entry.page.url()) !== boundTarget.identity)) {
          armed = false;
          event = null;
          setBoundTargetState("DISARMED");
        }
      });
      if (typeof entry.page.route === "function") await entry.page.route("**/*", async (route) => {
        if (route.request().isNavigationRequest() && route.request().frame() === entry.page.mainFrame() && !isAllowedBoundNavigation(route.request().url())) await route.abort("blockedbyclient");
        else await route.continue();
      });
      configureCallbackTarget({ targetId: target.callback_target_id });
      sendJson(response, 200, { message: "已完成 Bind、Arm，并已写入 .env。", callback_target_id: target.callback_target_id, browser_context_id: contextDescriptor.id });
    } catch (error) {
      sendJson(response, 400, { error: error.message || "bind_arm_configure_failed" });
    }
    return true;
  }
  const server = http.createServer((request, response) => { handleBindArmConfigure(request, response).then((handled) => { if (!handled) handle(request, response); }).catch((error) => sendJson(response, 500, { error: error.message || "request_failed" })); });
  return { server, listen: () => new Promise((resolve, reject) => { server.once("error", reject); server.listen(port, host, () => { boundState().then(resolve, reject); }); }), close: () => new Promise((resolve, reject) => server.close((error) => error ? reject(error) : resolve())) };
}
async function start() { const [, , mode, ...args] = process.argv; let attached; if (mode === "runtime") { const runtime = new BrowserRuntimeRegistry().load(); const { chromium } = require("playwright"); const result = await attachToExistingBrowser({ chromium, endpoint: runtime.cdp_endpoint }); attached = { ...result, profilePath: runtime.profile_path, close: () => result.browser.close() }; } else if (mode === "managed") { const { chromium } = require("playwright"); attached = await createRelayManagedBrowser({ chromium, ...parseManagedArguments(args) }); } else if (mode === "attach") { const selection = parseAttachArguments(args); if (selection.mode === "channel") attached = await attachViaPlaywrightCli(selection); else { const { chromium } = require("playwright"); const result = await attachToExistingBrowser({ chromium, endpoint: selection.endpoint }); attached = { ...result, close: () => result.browser.close() }; } } else throw new Error("usage: npm run browser-callback -- runtime | managed | attach"); const browserContext = attached.browserContext || createBrowserContextDescriptor({ context: attached.context, ownership: "attached_user_browser" }); const relay = createCallbackServer({ context: attached.context, browserContext, browserTarget: attached.browserTarget, pageBinding: attached.pageBinding, pageBindingStore: attached.pageBindingStore, profilePath: attached.profilePath, refresh: attached.adapter ? attached.adapter.refresh : undefined }); await relay.listen(); console.log(`[READY] ${browserContext.ownership}; browser context ${browserContext.id}; control page: http://127.0.0.1:${DEFAULT_PORT}`); const close = async () => { await relay.close(); await attached.close(); }; process.once("SIGINT", () => close().finally(() => process.exit(0))); process.once("SIGTERM", () => close().finally(() => process.exit(0))); }
if (require.main === module) start().catch((error) => { console.error(`[ERROR] ${error.message}`); process.exitCode = 1; });
module.exports = { BOUND_NAVIGATION_ORIGINS, BROWSER_CONTEXT_OWNERSHIPS, CALLBACK_REVIEW_INSTRUCTION, CALLBACK_STATUSES, CHANNELS, CHATGPT_PAGE_ORIGINS, DEFAULT_MANAGED_PROFILE_PATH, DEFAULT_MANAGED_START_URL, EVENT_SCHEMA, attachToExistingBrowser, attachViaPlaywrightCli, cliLaunchSpec, createBrowserContextDescriptor, createBrowserTargetDescriptor, createCallbackServer, createCliContext, createRelayManagedBrowser, extractChatInputRef, extractSnapshotTextboxRef, extractTabs, extractUrls, isAllowedBoundNavigation, isAllowedChatGPTOrigin, logManagedBrowserLifecycle, managedAuthenticationState, managedContextId, npxCliEntrypoint, parseAttachArguments, parseManagedArguments, renderAgentRelayEvent, resolveChatComposer, runPlaywrightCli, targetIdentity, validateAttachEndpoint, validateAttachedBrowser, validateBrowserContextId, validateBrowserContextOwnership, validateCallback, validateManagedProfilePath, validateManagedStartUrl, validateRelayCallbackUrl };
