"use strict";
const assert = require("assert"); const fs = require("fs"); const os = require("os"); const path = require("path"); const { setBrowserCallbackTarget } = require("./set-browser-callback-target");
const root = fs.mkdtempSync(path.join(os.tmpdir(), "agent-relay-target-")); try { const envPath = path.join(root, ".env"); fs.writeFileSync(envPath, "A=1\nAGENT_RELAY_BROWSER_CALLBACK_TARGET_ID=target-old\n"); setBrowserCallbackTarget({ targetId: "target-new-123", envPath }); const text = fs.readFileSync(envPath, "utf8"); for (const expected of ["A=1", "AGENT_RELAY_BROWSER_CALLBACK_TARGET_ID=target-new-123", "AGENT_RELAY_BROWSER_CALLBACK_URL=http://127.0.0.1:8787/api/callback", "AGENT_RELAY_BROWSER_EXECUTABLE_PATH=C:\\Program Files\\Google\\Chrome\\Application\\chrome.exe", "AGENT_RELAY_BROWSER_CDP_PORT=9333"]) assert(text.includes(expected)); assert.throws(() => setBrowserCallbackTarget({ targetId: "bad", envPath }), /callback_target_id_invalid/); } finally { fs.rmSync(root, { recursive: true, force: true }); } console.log("browser callback target config regression passed");
